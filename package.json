{
  "name": "@stayer/engine",
  "version": "0.0.1",
  "description": "HTTP requests performing module of the Stayer framework.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anton Bakhurynskyi",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^9.4.5",
    "typescript": "^2.7.1"
  },
  "dependencies": {
    "@stayer/interfaces": "0.0.14"
  }
}
